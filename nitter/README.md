# Nitter

This is currently broken because nitter requires an account now (twitter has nearly removed anonymous access).
Unfortunately, the script to export account cookies doesn't work (I'm missing a cookie called "auth_token" when I login).